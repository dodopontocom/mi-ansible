# file: main.yml 
---
    - name: update all apt
      hosts: localhost
    
      tasks:
      - name: update yum
        shell: yum -y update

#https://docs.ansible.com/ansible/latest/modules/yum_module.html